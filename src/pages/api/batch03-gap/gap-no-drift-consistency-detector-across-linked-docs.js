import retiredGapRoute from '@/lib/retired-gap-route';

export default retiredGapRoute(
  '/api/governance/jobs',
  'Queue the CONSISTENCY_REVIEW typed tool against permission-checked indexed sources.'
);
