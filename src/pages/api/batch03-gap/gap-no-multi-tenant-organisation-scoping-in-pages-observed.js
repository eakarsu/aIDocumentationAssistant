import retiredGapRoute from '@/lib/retired-gap-route';

export default retiredGapRoute(
  '/api/governance/jobs',
  'Tenant membership is now enforced directly by governed repository and job endpoints.'
);
