import retiredGapRoute from '@/lib/retired-gap-route';

export default retiredGapRoute(
  '/docs/repositories',
  'Use durable webhook events plus governed job status, retry, cancellation, and trace records.'
);
