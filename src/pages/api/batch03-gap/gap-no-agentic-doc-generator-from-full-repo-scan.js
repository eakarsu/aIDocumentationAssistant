import retiredGapRoute from '@/lib/retired-gap-route';

export default retiredGapRoute(
  '/api/governance/jobs',
  'Use the tenant-scoped repository sync and governed documentation job workflow.'
);
