import retiredGapRoute from '@/lib/retired-gap-route';

export default retiredGapRoute(
  '/settings',
  'The generic prompt did not manage tokens and was removed; no public API token feature is claimed.'
);
